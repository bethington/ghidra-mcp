# Semantic ground truth for src/str_len.c::compute_str_len.

function: compute_str_len

name_synonyms:
  - compute_str_len
  - ComputeStrLen
  - StrLen
  - string_length
  - StringLength
  - calc_str_len
  - CalcStrLen
  - strlen_impl
  - my_strlen

canonical_plate: |
  Computes the length of a null-terminated byte string. Walks the
  input pointer one byte at a time until a zero byte is found, then
  returns the distance from the start. Equivalent to the standard C
  library's strlen() but implemented in-place (does not call into
  the CRT). Behavior on a non-null-terminated buffer is undefined.

algorithm_tag: strlen
algorithm_synonyms:
  - strlen
  - string length
  - null-terminated
  - null terminator
  - byte walk
  - pointer walk

weights:
  name: 0.225
  signature: 0.225
  plate: 0.18
  algorithm: 0.135
  locals: 0.135
  # Mechanical contradiction checks (falsify.py's benchmark analog): the
  # doc must not CLAIM things that are provably false — a plate documenting
  # a nonexistent parameter, or a void/non-void return contradiction.
  falsifiability: 0.10
